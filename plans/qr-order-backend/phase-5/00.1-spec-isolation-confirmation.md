# Phase 5 Task 0.1 Spec Isolation Confirmation

Date: 2026-07-08
Task: `0.1 [P0] Confirm Phase 5 spec isolation`
Spec ID: `selkop-phase-5-frontend-integration`

## Audit

Read Phase 5 docs:

- `plans/qr-order-backend/phase-5/goal.md`
- `plans/qr-order-backend/phase-5/requirements.md`
- `plans/qr-order-backend/phase-5/tasks.md`

Checked expected Phase 5 source docs:

- `plans/qr-order-backend/phase-5/spec.yaml` is missing.
- `plans/qr-order-backend/phase-5/design.md` is missing.

Read Phase 4/backend authority docs:

- `docs/backend/05-api-spec/public-storefront-api.md`
- `docs/backend/11-sprint/implementation-status.md`

## Map

Task 0.1 is documentation-only preflight and maps to confirmation, not runtime implementation.

Classification: reuse existing Phase 5 docs and Phase 4 backend authority docs.

Reason: `requirements.md`, `tasks.md`, and backend authority docs already define the Phase 5 boundary. Missing `spec.yaml` and `design.md` are recorded as missing source docs rather than inferred.

Confirmed scope and boundaries:

- Active Phase 5 spec in `goal.md`, `requirements.md`, and `tasks.md` is `selkop-phase-5-frontend-integration`.
- Phase 5 implementation mode is audit-driven greenfield frontend.
- Frontend role is intent sender, UX layer, state presenter, and contract consumer.
- Backend remains source of truth for price, totals, QR locked outlet/location, payment status, fulfillment status, public order status, and admin allowed actions.
- Online Store, QR Store, Cart, Checkout, Payment Pending, Public Order Tracking, Admin Order Management, Admin QR Management, and minimal Admin Payment Provider Settings are in Phase 5 scope.
- Existing WhatsApp/admin frontend flows must not regress.

Phase 4/backend authority evidence:

- `docs/backend/05-api-spec/public-storefront-api.md` states public routes ignore client-provided totals, prices, payment status, fulfillment status, and workspace ID.
- The same API spec states outlet/location/table QR checkout locks outlet/location from the QR token and rejects overrides.
- Checkout requires `Idempotency-Key`, `customer.name`, and `customer.phone`.
- Payment status is read-only for frontend; `paid` only appears after backend/provider confirmation.
- Public order lookup must not expose internal IDs, raw provider payloads, secrets, internal notes, audit internals/logs, COGS, raw totals objects, or unmasked customer phone numbers.
- `docs/backend/11-sprint/implementation-status.md` records Phase 4 final status as documentation complete for alpha handoff, with local validation blocked and BayarGG live credential verification deferred.

## Plan

Files read:

- `plans/qr-order-backend/phase-5/goal.md`
- `plans/qr-order-backend/phase-5/requirements.md`
- `plans/qr-order-backend/phase-5/tasks.md`
- `docs/backend/05-api-spec/public-storefront-api.md`
- `docs/backend/11-sprint/implementation-status.md`
- `package.json`

Files created:

- `plans/qr-order-backend/phase-5/00.1-spec-isolation-confirmation.md`

Files modified:

- `plans/qr-order-backend/phase-5/tasks.md`

Tests to write first: not applicable; this task does not change runtime behavior.

Backend contract dependency: Phase 4 public storefront/QR/cart/checkout/payment/order/admin authority remains unchanged and must remain backend-owned during later Phase 5 tasks.

Regression impact: none expected because no frontend or backend runtime files are changed.

Blocked status: `spec.yaml` and `design.md` are missing from `plans/qr-order-backend/phase-5/`; this preflight continues under the user-approved blocker bypass and records them as missing source docs.

## RED Test

Not applicable.

Reason: task 0.1 confirms spec isolation and updates documentation only. No behavior-changing implementation was performed, so a failing behavior test would be artificial.

## GREEN / Confirmation Result

Confirmed:

- Phase 5 active spec identifier is `selkop-phase-5-frontend-integration`.
- Phase 5 is isolated to frontend integration and must not alter backend authority.
- Phase 5 implementation mode is audit-driven greenfield frontend.
- Frontend is limited to intent sender, UX layer, state presenter, and contract consumer.
- Backend authority from Phase 4 remains unchanged.
- Phase 5 includes Online Store, QR Store, Checkout, Payment Pending, Public Order Tracking, and Admin Operations.
- Existing WhatsApp/admin frontend flows must not regress.

Not confirmed from source because documents are missing:

- `spec.yaml`
- `design.md`

## Verify

Planned lightweight verification:

```bash
npm run specs:check
```

Result: not executed. The background process tool rejected the command because the active session command policy denies this shell-backed execution. No specs pass/fail result is claimed.

## Docs Update

Updated:

- Added this task 0.1 confirmation deliverable.
- Updated `plans/qr-order-backend/phase-5/tasks.md` task 0.1 checklist to mark confirmed items accurately.
- Added a short task 0.1 note documenting missing source docs and blocker bypass.

## Report

Summary:

- Task 0.1 was completed as documentation-only spec isolation confirmation.
- Missing `spec.yaml` and `design.md` were recorded and not invented.
- No unrelated Phase 5 UI/API changes were implemented.
- No broader Phase 5 tasks or checkpoints were marked complete.

Known blockers:

- `plans/qr-order-backend/phase-5/spec.yaml` missing.
- `plans/qr-order-backend/phase-5/design.md` missing.

Risks:

- Later Phase 5 design/implementation tasks may need `design.md` or equivalent design authority before UI structure decisions are finalized.
- Spec lifecycle tooling may not validate Phase 5 plan docs if this folder is outside the repository's active specs index.
