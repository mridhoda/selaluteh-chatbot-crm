# 05.13 Alpha Frontend Readiness

## Decision

Phase 5 Wave 6 result: **NO-GO for alpha approval**.

Phase 5 frontend implementation and documentation are ready to hand off to Phase 6 Alpha Testing & QA, but alpha GO cannot be claimed because final command verification, browser/E2E execution, manual sandbox checks, mobile/accessibility verification, and existing WhatsApp/admin/CRM regression smoke checks were not executed in this environment.

## Deliverable Audit

Required Phase 5 deliverables present:

- `05.0-existing-frontend-contract-audit.md`
- `05.1-frontend-gap-analysis-integration-map.md`
- `05.2-frontend-architecture-app-shell.md`
- `05.3-api-client-contract-integration.md`
- `05.4-public-online-store-ui.md`
- `05.5-qr-store-ui.md`
- `05.6-cart-menu-interaction.md`
- `05.7-checkout-payment-ui.md`
- `05.8-public-order-tracking-ui.md`
- `05.9-admin-order-management-ui.md`
- `05.10-admin-qr-payment-settings-ui.md`
- `05.11-frontend-security-error-state-handling.md`
- `05.12-frontend-testing-e2e-stabilization.md`
- `05.13-alpha-frontend-readiness.md`

Wave 0 support deliverables also present:

- `00.1-spec-isolation-confirmation.md`
- `00.2-current-frontend-baseline.md`
- `00.3-frontend-test-structure.md`
- `00.4-api-mock-contract-fixtures.md`
- `00.5-frontend-env-secret-guard.md`
- `00.6-phase-5-release-gates.md`

Status/limitation review:

- Existing Wave 0-5 docs record missing `spec.yaml`/`design.md`, shell/process command blocker, missing E2E/browser runner, missing manual sandbox path, and missing backend admin QR contract where applicable.
- No deliverable claims E2E, manual sandbox, mobile browser, or final alpha verification passed.
- No separate Phase 5 implementation status, progress log, or current task pointer file exists under `plans/qr-order-backend/`; final status is therefore recorded here and in `tasks.md` section 14.

## Implemented Scope

Implemented or documented through Wave 0-5 artifacts:

- Phase 5 audit, baseline, gap map, release gates, and test fixture layer.
- Centralized Phase 5 API client boundary and backend error mapping.
- Public storefront contract consumption and model-level UI safety behavior.
- QR store resolve/scope model behavior without client-side QR authority.
- Intent-only cart model, backend cart validation integration, checkout payload safety, and idempotency enforcement in API client/model tests.
- Payment pending/status read-only behavior with bounded polling and redirect-not-paid handling.
- Public order tracking sanitization for public-safe fields.
- Admin order list/detail/action model, API safety, tests, and non-active reference UI using backend `allowed_actions` and explicit lifecycle endpoints. Active `/app/orders` was restored to legacy `OrdersPage` by user request on 2026-07-08, so Phase 5 admin order UI integration is partial and must be added into existing UI/components instead of replacing the page.
- Admin payment settings read-only provider status display.
- Admin QR blocked-status documentation/UI because backend admin QR routes were not found.
- Security, privacy, error-state, duplicate-submit, polling, and intent-storage safeguards covered by Node-testable suites.

## Deferred Scope

Deferred or blocked before alpha GO:

- Browser/component DOM tests for public, QR, checkout, payment, tracking, admin, loading, error, empty, mobile, and accessibility states.
- E2E flows for Online Store, QR Store, Admin Order Lifecycle, and security/regression checks.
- Manual sandbox verification against staging/local Phase 4 backend contracts and BayarGG sandbox/fake provider mode.
- Existing admin/WhatsApp/CRM/chat regression smoke in a real browser session.
- Admin QR management list/detail/create/activate/disable/revoke/export until backend admin QR contract exists.
- Payment provider configuration mutations; only read-only provider status is in Phase 5 scope.

## Tests Added Across Phase 5

Node-testable Phase 5 suites present under `web/test/phase-5/`:

- `contract-fixtures.test.mjs`
- `env-guard.test.mjs`
- `public-store-ui-model.test.mjs`
- `qr-store-ui-model.test.mjs`
- `cart-checkout-payment-order-model.test.mjs`
- `admin-order-management.test.mjs`
- `admin-qr-payment-settings.test.mjs`
- `phase5-api-client-safety.test.mjs`

Covered by these suites according to Wave 0-5 docs:

- Contract fixture shape and authority boundaries.
- Forbidden frontend env secret names.
- Public store and QR model behavior.
- Cart intent-only persistence and backend validation modeling.
- Checkout idempotency, duplicate guard, and forbidden authority-field rejection.
- Payment redirect-not-paid and bounded polling model behavior.
- Public order privacy sanitization.
- Admin allowed action rendering from backend data only.
- Explicit admin lifecycle endpoints and duplicate action guard.
- Admin QR blocked contract state and payment settings secret stripping.
- API client auth/workspace/idempotency/QR token/error handling safety.

## Verification Commands

Final verification commands requested for alpha readiness:

```sh
npm --prefix web test
npm --prefix web run lint
npm --prefix web run build
npm run specs:check
```

Result: **not run** in this execution because the available toolset does not include a foreground shell command runner. Prior Wave 0-5 docs record the same workspace shell/process policy blocker. No pass result is claimed.

E2E/browser commands: **not run** because no E2E/browser runner is configured in `web/package.json`.

## Manual Sandbox Result

Manual sandbox result: **blocked, not executed**.

Required but unverified manual checks:

- Public Online Store checkout on desktop and mobile.
- Universal QR, Outlet QR, and Location QR flows.
- BayarGG sandbox or backend fake-provider payment pending/status behavior.
- Public order tracking from a real public token.
- Admin order processing with authenticated workspace context.
- Admin payment settings display against real backend response.
- Existing login, admin navigation, WhatsApp order visibility, CRM/chat, products, payments, and platforms regression smoke.
- Browser/device matrix and accessibility/keyboard focus checks.

## Final Frontend Alpha Checklist

Checklist result based on implemented code/docs and available verification:

- Public storefront usable: **blocked/unverified** without browser/manual sandbox execution.
- QR Store usable: **blocked/unverified** without browser/manual sandbox execution and backend sandbox QR sessions.
- Mobile layout acceptable: **blocked/unverified** without browser viewport pass.
- Cart validation works: **partially covered** by API/model code and tests; runtime backend verification blocked.
- Checkout idempotency works: **partially covered** by API/model code and tests; runtime/backend verification blocked.
- Payment pending is understandable: **partially covered** by code/docs; browser/manual verification blocked.
- Public order tracking works: **partially covered** by model/UI implementation; runtime/backend verification blocked.
- Admin order processing works: **partially covered** by model/API/tests and non-active reference UI; active legacy `/app/orders` still needs Phase 5 safety integration and authenticated E2E/manual verification.
- Admin QR/payment settings status: payment settings read-only status implemented; admin QR management blocked by missing backend contract.
- Error, loading, and empty states are clear: **partially covered** by code/docs; browser/mobile verification blocked.

## No-Go Condition Review

Review result based on implemented code/docs and available tests:

- Frontend cannot mark payment paid: **partially verified by code/docs/model tests; runtime unverified**.
- Frontend does not send `payment_status`: **partially verified by API/model tests; final command run blocked**.
- Frontend does not send `fulfillment_status`: **partially verified by API/model tests; final command run blocked**.
- Frontend does not treat redirect as paid: **partially verified by model tests/docs; browser flow unverified**.
- Frontend does not use local total as final authority: **partially verified by model tests/docs; browser flow unverified**.
- Frontend cannot override QR locked outlet/location: **partially verified by model/API tests/docs; backend sandbox QR flow unverified**.
- Checkout cannot submit without `Idempotency-Key`: **partially verified by API tests/docs; runtime unverified**.
- Duplicate checkout is prevented: **partially verified by model/UI docs; browser/backend idempotency behavior unverified**.
- Admin actions use explicit endpoints: **partially verified by API/model tests/docs; authenticated E2E unverified**.
- `allowed_actions` are not guessed: **partially verified by model tests/docs; browser rendering unverified**.
- Raw provider payload/internal order ID/admin notes do not appear in public UI: **partially verified by model tests/docs; browser rendering unverified**.
- Existing WhatsApp/admin flows do not break silently: **blocked/unverified** because regression smoke was not executed.

Because multiple No-Go checks are only partially verified or blocked, Phase 5 cannot be approved as alpha GO.

## Static No-Go Verification Bypass Review, 2026-07-08

Scope: static/tool-based review only. This review used source and documentation reads/grep and did not execute shell commands, npm scripts, a browser, E2E flows, or manual payment/backend sandbox checks. Result labels below mean:

- **Static Cleared**: current source code contains code-review-verifiable guardrails for the No-Go condition.
- **Partial**: source code reduces risk, but a UI/runtime ambiguity or unexecuted test path remains.
- **Blocked Runtime**: cannot be verified without browser/E2E/manual/backend execution.
- **Contract Blocked**: backend/admin contract is missing or not fully available for alpha verification.

Per-item static review:

| No-Go condition | Static status | Static evidence | Runtime gap |
|---|---|---|---|
| Frontend can mark payment paid | **Static Cleared** | Payment status is read from `phase5ApiClient.public.getPaymentStatus()` only in `web/src/features/public-store/hooks/usePaymentStatus.js:14-24`; checkout success navigates to pending by `paymentId`/token in `web/src/features/public-store/pages/CheckoutPage.jsx:24-30`; payment page states redirect is not paid in `web/src/features/public-store/pages/PaymentPendingPage.jsx:120-125`. | Browser/backend payment flow not executed. |
| Frontend sends `payment_status` | **Static Cleared** | API guard rejects backend-owned fields including `payment_status` in `web/src/features/public-store/api/phase5ApiClient.js:9-25` and recurses request bodies in `web/src/features/public-store/api/phase5ApiClient.js:52-66`; checkout calls this guard in `web/src/features/public-store/api/phase5ApiClient.js:130-137`. | Tests/npm not executed in this environment. |
| Frontend sends `fulfillment_status` | **Static Cleared** | Same API guard rejects `fulfillment_status`/`fulfillmentStatus` in `web/src/features/public-store/api/phase5ApiClient.js:9-25` and checkout/admin action payloads are checked in `web/src/features/public-store/api/phase5ApiClient.js:130-148`. | Tests/npm not executed in this environment. |
| Frontend treats redirect as paid | **Static Cleared** | Payment URL is normalized as `paymentUrl` only, while `paymentStatus` comes from backend status in `web/src/features/public-store/utils/cartIntentModel.js:159-172`; opening the provider URL only uses `window.open()` in `web/src/features/public-store/pages/PaymentPendingPage.jsx:24-28`; pending page text says redirect gateway is not paid in `web/src/features/public-store/pages/PaymentPendingPage.jsx:120-125`. | Browser/provider redirect flow not executed. |
| Frontend uses local total as final authority | **Static Cleared** | Checkout payload includes context/customer/items only in `web/src/features/public-store/utils/cartIntentModel.js:62-72`; local preview is tagged `local_preview_only` in `web/src/features/public-store/utils/cartIntentModel.js:93-106`; checkout UI labels totals as estimate/backend validated in `web/src/features/public-store/pages/CheckoutPage.jsx:299-335`. | Backend amount reconciliation not executed. |
| Frontend can override Outlet QR locked outlet | **Static Cleared** | QR page derives lock/session from backend resolve in `web/src/features/public-store/pages/QrStorePage.jsx:50-59`; locked QR disables outlet selection in `web/src/features/public-store/pages/QrStorePage.jsx:72-89`; `includeOutlet` is false for locked flows via `useGuestCart()` context in `web/src/features/public-store/pages/QrStorePage.jsx:74` and checkout query in `web/src/features/public-store/pages/QrStorePage.jsx:170-176`; override assertion runs before add-to-cart in `web/src/features/public-store/pages/QrStorePage.jsx:154-160`. | Backend QR sandbox not executed. |
| Frontend can override Location QR locked location | **Static Cleared** | Location lock label comes from backend model in `web/src/features/public-store/pages/QrStorePage.jsx:26-30`; QR intent context is created from normalized QR model in `web/src/features/public-store/pages/QrStorePage.jsx:117-120`; override assertion checks selected outlet/location in `web/src/features/public-store/pages/QrStorePage.jsx:154-160`. | Backend QR sandbox not executed. |
| Checkout can submit without `Idempotency-Key` | **Static Cleared** | Client throws without idempotency key in `web/src/features/public-store/api/phase5ApiClient.js:130-137`; checkout form creates/reuses an attempt key before submission in `web/src/features/public-store/hooks/useCheckoutForm.js:44-54`. | Network/runtime verification not executed. |
| Duplicate checkout can happen from UI | **Partial** | `useCheckoutForm()` returns early when `submitting` is true and resets the flag in `web/src/features/public-store/hooks/useCheckoutForm.js:27-63`; checkout button is disabled while submitting in `web/src/features/public-store/pages/CheckoutPage.jsx:347-356`. | Needs browser interaction/race testing and backend idempotency verification. |
| Admin action uses generic PATCH status | **Static Cleared** | Admin client uses explicit POST lifecycle endpoints only in `web/src/features/public-store/api/phase5ApiClient.js:147-178`; static grep did not identify Phase 5 admin order generic PATCH status use. | Authenticated backend E2E not executed. |
| Admin can complete unpaid order from UI | **Partial** | Phase 5 admin model/API/reference UI renders actions only from backend `allowed_actions` in `web/src/modules/orders/models/adminOrderModel.js:58-95` and `web/src/modules/orders/pages/Phase5AdminOrdersPage.jsx`; complete action maps only when backend permits `mark_completed` in `web/src/modules/orders/models/adminOrderModel.js:1-15`. | Active `/app/orders` was restored to legacy `OrdersPage` on 2026-07-08; legacy `OrderQuickActions` still has status-derived complete/preparing actions in `web/src/modules/orders/components/OrderQuickActions.jsx:18-78`, and backend `ORDER_UNPAID` enforcement was not executed. |
| `allowed_actions` are guessed client-side | **Partial** | Phase 5 reference page uses `getRenderableAdminActions(selectedOrder)` from backend-normalized `allowedActions`, and unknown actions are filtered in `web/src/modules/orders/models/adminOrderModel.js:58-95`. | Active `/app/orders` now uses legacy `OrdersPage`; legacy quick-action code still guesses transitions by status and must be integrated with backend `allowed_actions` before this can clear. |
| Raw provider payload appears in public UI | **Static Cleared** | Public order sanitizer allowlists public keys in `web/src/features/public-store/utils/cartIntentModel.js:6-23` and returns sanitized fields only in `web/src/features/public-store/utils/cartIntentModel.js:218-246`; public order page renders sanitized order fields in `web/src/features/public-store/pages/OrderStatusPage.jsx:65-111`. | Browser rendering not executed. |
| Internal order ID appears in public UI | **Partial** | Public order page displays `order.orderNumberPublic` in `web/src/features/public-store/pages/OrderStatusPage.jsx:65-69`, and sanitizer maps public token/number only in `web/src/features/public-store/utils/cartIntentModel.js:218-246`. However, payment pending page derives and displays `#PAY-${paymentId}` from the route token in `web/src/features/public-store/pages/PaymentPendingPage.jsx:34-35` and `web/src/features/public-store/pages/PaymentPendingPage.jsx:152-167`, so this cannot be statically cleared without confirming `paymentId` is public-safe. | Needs contract confirmation that pending route `paymentId` is public-safe or UI should stop displaying it. |
| Admin notes appear in public UI | **Static Cleared** | Public sanitizer does not allow admin note keys in `web/src/features/public-store/utils/cartIntentModel.js:6-23` and public order page renders only sanitized customer/status/order summary fields in `web/src/features/public-store/pages/OrderStatusPage.jsx:83-111`. | Browser rendering not executed. |
| QR mismatch is unclear to user | **Partial** | QR error and empty-outlet states use explicit messages in `web/src/features/public-store/pages/QrStorePage.jsx:100-113`; locked/selection guidance is shown in `web/src/features/public-store/pages/QrStorePage.jsx:137-142`. | Browser/mobile UX not executed; mismatch cases require seeded QR scenarios. |
| Payment failed/expired state is unclear to user | **Partial** | Payment labels include failed/expired/manual review in `web/src/features/public-store/utils/cartIntentModel.js:175-184`; payment status is displayed on pending page in `web/src/features/public-store/pages/PaymentPendingPage.jsx:197-200`. | Browser/manual provider failure and expiry scenarios not executed. |
| Existing WhatsApp/admin flow breaks silently | **Blocked Runtime** | Static routing preserves public routes outside `/app/*` in `web/src/routes/privateRoutes.jsx:26-32` and imports Phase 5 admin orders into dashboard in `web/src/modules/dashboard/pages/DashboardPage.jsx:22`; navigation still includes Orders/Chat/Platforms entries in `web/src/routes/navigation.config.js:1-48`. | Existing WhatsApp/admin/CRM regression smoke requires browser/manual execution and remains unverified. |

Overall static bypass result: several authority/privacy No-Go items are code-review-verifiable and statically cleared, but runtime, browser, backend sandbox, existing regression checks, and active legacy admin Orders UI integration remain incomplete. The alpha decision remains **NO-GO pending runtime verification and legacy Orders UI integration**. This static review reduces risk only; it does not replace E2E/manual sandbox approval.

## Known Limitations

- No command execution results are available for tests, lint, build, or spec sync.
- No E2E/browser runner exists in the web package.
- No manual sandbox browser/backend/payment-provider session was available in this environment.
- Component behavior is represented by Node-testable model/API tests, not DOM assertions.
- Admin QR management remains blocked by missing backend admin QR routes.
- Existing WhatsApp/admin/CRM regression is documented as required but unverified.

## Risks and Follow-Up

Primary risks before alpha approval:

- Undetected runtime integration failures in public checkout/payment/tracking/admin order flows.
- Active legacy `/app/orders` does not yet include Phase 5 backend `allowed_actions`, explicit lifecycle endpoints, duplicate-submit protection, cancel reason validation, and safe admin lifecycle error handling.
- Undetected responsive/mobile/accessibility issues.
- Undetected regression in existing admin, WhatsApp, CRM/chat, products, payments, or platforms screens.
- Backend contract drift between model/API assumptions and staging responses.
- Admin QR management cannot be validated until backend exposes safe admin QR endpoints.

Recommended Phase 6 Alpha Testing & QA entry task:

1. Provision a runnable frontend/backend sandbox with seeded Phase 4 data, BayarGG sandbox or fake-provider mode, authenticated admin workspace, and a browser/E2E runner.
2. Run `npm --prefix web test`, `npm --prefix web run lint`, `npm --prefix web run build`, and `npm run specs:check`.
3. Execute the `05.12` E2E/manual matrix and update this readiness decision only after all No-Go conditions are verified as cleared.

## Handoff Status

Phase 5 is finalized for documentation/reporting handoff to Phase 6.

Phase 6 must treat the current state as **implementation present, verification incomplete, alpha NO-GO until proven otherwise**.
