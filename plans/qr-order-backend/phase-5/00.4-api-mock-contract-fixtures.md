# 0.4 API Mock and Contract Fixture Layer

Task: `0.4 [P0] Create API mock and contract fixture layer`

Date: 2026-07-08

Scope: Test fixture layer only. Fixtures are not imported by runtime frontend code.

## Audit

Contract evidence inspected:

- Public routes: `server/src/routes/public-store.js`
- Public storefront/cart/checkout/payment service: `server/src/services/public-storefront.service.js`
- Public order tracking service: `server/src/services/public-order.service.js`
- QR context service: `server/src/services/qr-order-session.service.js`
- Admin order contract: `server/src/routes/admin-orders.js`
- Existing frontend mock public-store implementation: `web/src/features/public-store/api/publicStoreApi.js`, `web/src/features/public-store/data/publicStore.mock.js`

## Map

Fixture coverage added in `web/test/phase-5/fixtures/api-contract-fixtures.mjs`:

- Public storefront response.
- QR resolve response with outlet/location lock context.
- Cart validation request and response.
- Checkout request and response with `Idempotency-Key` fixture.
- Payment status response.
- Public order tracking response.
- Admin order list, detail, explicit action request, and action response.
- Admin QR settings fixture, marked documentation/mock-only until a backend route is confirmed.
- Admin payment settings fixture for UI test preparation, aligned with public-safe provider display/config concepts.
- Critical backend error fixtures.

Authority boundary:

- Request fixtures intentionally omit `payment_status`, `fulfillment_status`, `allowed_actions`, `final_total`, and generic `status` patching.
- Backend-owned fields appear only in response fixtures to help the frontend render backend decisions.
- `allowedActions`/`allowed_actions` are response-only and must not be guessed by frontend tests.

## Plan

Files created:

- `web/test/phase-5/fixtures/api-contract-fixtures.mjs`
- `web/test/phase-5/contract-fixtures.test.mjs`

Regression impact:

- None at runtime.

## Verify

Command execution remains blocked by the workspace process policy documented in `00.2-current-frontend-baseline.md`.

## Known Limitations

- Admin QR settings backend route was not found in `server/src/routes/*qr*`; fixture is for future frontend tests only.
- Exact DTO names are mixed camel/snake in existing backend/public services; later API client work must normalize from confirmed responses rather than rely solely on these fixtures.
